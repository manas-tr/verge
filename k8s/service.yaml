apiVersion: v1
kind: Service
metadata:
  name: verge-service
spec:
  type: NodePort
  selector:
    app: verge
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30007
